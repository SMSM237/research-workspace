# 위젯 0.3.3 검증

- 원인: 추가 버튼과 입력창이 회의 탭을 구분하지 않아 할 일 저장 함수를 항상 호출했습니다. 기존 0.3.2에서 회의 탭 → 추가 → 할 일 입력창을 실제 재현했습니다.
- 수정: 위젯별 탭을 입력창에 전달하고, 회의는 제목·날짜·시간을 받아 Meetings/Schedule에 meeting_schedule 노트로 저장합니다. 할 일 추가·수정은 기존 경로를 유지합니다.
- PASS: JVM 3개 검증 프로그램. 기존 할 일 날짜/메타데이터/동시 수정 보호, 회의 속성/체크, 탭별 분기, 한국어·따옴표·역슬래시 제목 왕복, 잘못된 날짜·시간·입력 거부.
- PASS: Android API36 기존 시험용 앱에 업데이트 설치. 회의 추가 후 회의 목록 표시, 할 일 파일 바이트 보존, 할 일 추가·수정창·삭제, 재실행 후 회의 재표시.
- PASS: 실제 날짜·시간 선택창으로 2026-09-24 15:30 선택 → 저장 → 파일 readback. 기존 할 일 파일 보존.
- PASS: 기존 APK와 같은 서명 SHA256 30dd534c6ef24b8b57f70e1514d5b1a1908fc4a231baf8be03e707b617a59c80. 위젯 배치와 Vault 권한 유지.
- FULL 디자인 검토: 기존 작은 입력 카드·색·아이콘·버튼 스타일 유지, 제목/본문/날짜/시간/저장 순서와 키보드 표시 시 잘림 없음. 제목/입력/날짜/시간의 접근성 설명 확인. 새 모션/그래프 계산/논문 분석은 해당 없음. 사용자 기기의 실제 런처, 큰 글씨·가로 화면, 화면 회전 중 입력 복원은 미검증입니다. 회전 복원 코드는 포함했습니다.
- 개인 Vault, 실제 회의/할 일, Git 자격증명은 시험·공개 패키지에 포함하지 않았습니다.

사용: APK를 기존 앱 위에 설치하세요. Git 동기화만으로 앱 바이너리는 업데이트되지 않습니다. 회의 탭 → 추가 → 제목·날짜·시간 → 추가하기. 과거에 잘못 저장된 할 일은 자동 변환하지 않습니다.

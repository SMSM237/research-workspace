# 검증 범위 · 2026-09-15

## 대시보드 0.7.3

- PASS: Node 테스트 67개와 Obsidian SDK 타입 검사. 할 일 수정·삭제·이월, 미래 일정 집계, 회의록 삭제 후 재연결, 주차 이동, 그래프 노드, 분석 상태와 요청 중복 방지 포함.
- PASS: 실제 Windows Obsidian에서 체크 표시 중앙 좌표, 활성 플러그인 버전, 제목–주차 간격 8px 및 날짜별 구절 변경 확인.
- PASS: 공개판 모듈 이름과 모든 모바일 홈 링크, 실행 파일 세 개 및 스타일 포함 여부 검사.
- UNVERIFIED: 이번 공개판을 새 Mac/실제 Android에서 직접 설치하는 전 과정. 기존 화면 테스트와 코드 검사를 해당 기기의 실제 검증으로 간주하지 않음.

## 분석기와 설치

- PASS: Python 201개 테스트. 원문 처리, PDF 대기열, Markdown/HTML 생성, 구조 검증, 보충자료와 표, 중복 분석 방지, 변경된 파일 보호, 배포 receipt 검증 포함.
- PASS: 새 설치 → config 저장/재읽기 → PDF 두 개 감지 → 재검사 시 중복 없음 → AI hold 상태 유지. 모델 실행/업로드 없이 합성 PDF로 검사.
- PASS: 기존 설치 폴더 보존, 잘못 선택한 Vault 루트 거부, 동기화 폴더 안의 개인 설정 설치 거부, URL에 인증 정보를 넣는 방식 거부.
- PASS: 공개 수신기의 전송 대상은 설치자의 로컬 expected_remote와 일치해야 함. 요청 JSON에서 전송 대상을 가져오지 않음.
- SKIPPED 2: Windows 계정의 symbolic-link 생성 권한이 없어 해당 symlink 테스트 두 개 제외. 별도의 Windows junction 탈출 테스트는 실행됨.
- UNVERIFIED: 다른 사용자의 ChatGPT/Codex 로그인, Git 계정, 모바일→Git→PC 유휴 Codex 자동 재개 전 과정. 실제 사용량 제한과 UI 차이는 설치 환경에서 진단 필요.
- N/A: 과학적 새 분석 결과의 정확도 평가. 이 배포에서는 실제 논문 분석을 실행하지 않았음.

## 위젯 0.3.2

- PASS: Java/Android 빌드, 기존 APK와 동일 서명, 설치 업데이트, 4×2/3×2 크기, 글꼴 130%에서 헤더, 할 일 추가·수정·삭제 후 파일 재읽기와 원문 복원(0.3.1에서 검사한 동일 데이터 경로).
- PASS: 네이티브 프레임에서 진행 방향의 물결 경계, 아이콘과 글자의 중앙 수렴, 체크와 방사선 효과 검사. 변경된 구간만 채우거나 비우며 정지 상태는 정확한 완료 비율을 표시함.
- PASS: 에뮬레이터에서 동기화·분석 요청과 할 일 체크를 녹화하여 재생/복귀를 확인. 요청 전달은 QA 전용 수신기로 검사했으며 실제 Git 서버 완료로 표시하지 않음.
- PASS: 모션 감소 설정에서는 정적 피드백 사용. 네이티브 애니메이션은 상태 변경 시에만 전달하고 매 프레임을 IPC로 전송하지 않음.
- UNVERIFIED: 사용자 휴대전화에서의 체감 부드러움과 제조사별 런처 동작. 20ms/10ms 리소스 프레임 간격은 실기기 FPS 보증이 아님.

## 공개 패키지

- 개인 Vault의 실제 노트·원본 PDF·리포트·Chat 기록·인증 파일·서명 키·실행 ledger를 제외한 허용 목록으로 생성.
- 소스 텍스트 및 ZIP의 파일 목록·비밀정보 패턴, SHA-256, ZIP 재열기 검사.
- MIT 라이선스는 자체 코드에 적용. 제3자 의존성·인용 본문은 원래 권리 유지.
